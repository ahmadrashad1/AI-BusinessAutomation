version: '3.9'
# Production overrides
services:
  api:
    command: uvicorn app.main:app --host 0.0.0.0 --port 8000 --workers 4
    restart: always
  worker:
    restart: always
  beat:
    restart: always
  frontend:
    command: npm start
    restart: always
  nginx:
    restart: always
